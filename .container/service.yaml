apiVersion: v1
kind: Service
metadata:
  name: mcp-salesapirest
  labels:
    app: mcp-salesapirest
spec:
  type: ClusterIP
  selector:
    app: mcp-salesapirest
  ports:
    - name: sse
      port: 8080
      targetPort: sse
      protocol: TCP
